# hospital-database-project
Hospital Management System database project using PostgreSQL. Features schema design, data import, CRUD operations, joins, aggregations, window functions, and analytical SQL queries on a real-world healthcare dataset.
